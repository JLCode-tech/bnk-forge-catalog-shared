---
description: Quick project health check — git, active work, backlog, tests
agent: build
---

Load the `status` skill and run it.

Show git state, active work from `.agent/CURRENT_WORK.md`, backlog from `.agent/backlog/BACKLOG.md`, and a quick health check (lint + tests). Report results concisely.

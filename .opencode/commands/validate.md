---
description: Run all quality gates — lint, types, tests, build
agent: build
---

Load the `validate` skill and run it.

Run the full quality gate: lint, type check, tests, build. Report pass/fail for each step. If anything fails, show the first error clearly.
